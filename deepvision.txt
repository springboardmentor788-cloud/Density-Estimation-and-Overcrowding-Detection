import cv2
import os

path = "dataset/ShanghaiTech/part_B_final/train_data/images"

files = os.listdir(path)
img_path = os.path.join(path, files[0])

img = cv2.imread(img_path)

cv2.imshow("Dataset Test", img)
cv2.waitKey(0)
cv2.destroyAllWindows()